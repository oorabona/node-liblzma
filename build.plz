load "coffee"
load "mocha"

task 'bindings',
  description: 'Compile liblzma bindings for NodeJS'
  attach: 'build'
  run: ->
    exec('node-gyp configure build').then ->
      notice 'Build complete'

settings.clean = [ "lib", "build" ]
settings.distclean = [ "node_modules" ]
